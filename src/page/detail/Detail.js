import React from "react";

function Detail({ loading, movie, error }) {
  console.log(movie);
  console.log(error);
  return (
    <div>
      <h1>Detail</h1>
    </div>
  );
}

export default Detail;
